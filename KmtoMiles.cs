/* using System;
class distance
{
    static void Main()
    {
        // Distance in kilometers
        double distanceInKilometers = 10.8;
        // Convert kilometers to miles (1 km = 1.6 miles)
        double distanceInMiles = ConvertKilometersToMiles(distanceInKilometers);


        // Display the result
        Console.WriteLine($"The distance {distanceInKilometers} km in miles is {distanceInMiles:F2}");
    }


    // Method to convert kilometers to miles
    static double ConvertKilometersToMiles(double kilometers)
    {
        return kilometers / 1.6;
    }
}


 */