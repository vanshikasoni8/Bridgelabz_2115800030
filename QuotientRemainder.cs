/*using System;

class Division
{
    // Function to calculate the quotient
    static int GetQuotient(int number1, int number2)
    {
        return number1 / number2;
    }

    // Function to calculate the remainder
    static int GetRemainder(int number1, int number2)
    {
        return number1 % number2;
    }

    static void Main()
    {
        Console.Write("Enter the first number: ");
        int number1 = Convert.ToInt32(Console.ReadLine());

        Console.Write("Enter the second number: ");
        int number2 = Convert.ToInt32(Console.ReadLine());

        // Call functions to perform the calculations
        int quotient = GetQuotient(number1, number2);
        int remainder = GetRemainder(number1, number2);

        // Display the results
        Console.WriteLine($"The Quotient is {quotient} and Remainder is {remainder} of two numbers {number1} and {number2}");
        
    }
} */